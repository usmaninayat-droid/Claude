# Defect log — recurring defect classes → prevention rules (promote to gate scripts)
#
# This IS the defect-taxonomy counter for the defect→gate promotion rule (see
# AGENT-TEAM-ARCHITECTURE §"defect→gate"). `count` = times this class has been seen.
# RULE: on the 2nd occurrence of a class whose `gate` is still "none", the conductor
# auto-opens a "promote <class> to gate" ticket. Bump `count` whenever a ticket of
# this class is filed; set `status` when a gate exists.

| class | count | example | prevention rule | gate | status |
|-------|-------|---------|-----------------|------|--------|
| ds-hardcoded-domain-assumption | 3 | DS chrome bakes domain defaults that generalize poorly: FleetHeader labels hardcoded 'Vehicle/Activity/Speed' (T-017); ReportsHome auto-selects first category landing (T-016); live-monitoring list excludes kind:'site' entities (ifm-workforce found); kind='asset' monitoring assumes all products are fleet/asset-based, not workforce/site generic. Same class as T-016/T-017 uncovered during ifm-workforce QA. | shared app-shell/navigation chrome must not bake fleet VOCABULARY a product can't override; expose it as a configurable prop whose fallback is the fleet word (`labels?.entity ?? 'Vehicle'`); opt out an intrinsic case with `// vocab-allow <word> — <reason>` | vocab.mjs (G-VOCAB) | RESOLVED T-019 — SI-4 promotion delivered. Static scan of `src/components/app-shell/**` + `navigation/**` for fleet words [Vehicle/Vehicles/Speed/Driver/Fleet/Truck/Depot/Plate] reaching the UI as V1 JSX text nodes or V2 default-label string literals; the sanctioned overridable fallback `?? '<word>'` is NOT flagged (T-017 pattern); comments/type-names never flagged; blocks/showcase/stories/tests excluded. Red-checked (seeded `'Fleet Depot'` + `<span>Plate Number</span>` → FAIL(4) w/ file:line, exit 1; reverted → PASS, exit 0). The non-regex-able half (default-landing + domain-MODEL `kind`-branching assumptions) stays a review-checklist item in review-fix. Wired into PostToolUse + review-fix + conductor gate chain. |
| single-filter | 1 | Only one filter facet next to search | shell renders all `deriveFilters` facets (multi-facet) | none (feature-level; not regex-able) | RESOLVED T-002 — behaviour covered by G-SMOKE once it lands |
| raw-hex | 1 | Hex literal in a component | token-only; grep `#` hex in src/components | coherence.mjs | ACTIVE |
| built-in-DS | 0 | Product files written under the DS | no product/recipe files under `V5 Design System - Ai/` | coherence.mjs | ACTIVE |
| svg-false-positive | 1 | fill="#fff" in inline SVG flagged as UI hex | skip data:image/viewBox/<svg/<path/fill=/stroke= | coherence.mjs | ACTIVE (calibration) |
| missing-a11y-name | 1 | icon-only <button>/<a> with no aria-label · <img> w/o alt · positive tabIndex | control needs an accessible name; img needs alt="" if decorative | a11y-static.mjs | ACTIVE (SI-5; brace/quote-aware scan, calibrate via `// a11y-allow`) |
| drop-on-save-round-trip | 4 | a *Sheet collects fields but the consumer's onSubmit/edit persists only some (CategorySheet dropped color/entityIds; RoleSheet edit persisted only name; AppSheet's `submitApp` dropped modules/subOrgIds/roleIds on create+edit; EventConfig dropped triggerPoint) → data loss | consumer save handler must read EVERY top-level Draft field as `d.<field>` OR forward the draft wholesale (`...d` / `[id]: d`) | roundtrip.mjs (G-ROUNDTRIP) | RESOLVED T-007 — SI-4 promotion delivered at 4th occurrence. Static scan of `blocks/**/*.block.tsx`: each `(d: XxxDraft) =>` handler must cover every top-level Draft field (minus `id`) or persist `d` wholesale; opt out a genuinely-transient field with `// roundtrip-allow <field>`. Red-checked against the AppDraft subOrgIds drop; wired into PostToolUse + review-fix + conductor gate chain. |
| drop-on-load-hydration | 3 | Editing a SEEDED row with no stored draft under-hydrates `initial`, so Save either overwrites real data or (worse) the wizard's step-gate leaves the submit button permanently DISABLED. Occurrences: EntityConfig (fieldCount→~0), UserAccounts (phone double-prefix `+971 +97…` — a CORRUPTION variant), PipelineConfig (seed had no `stages` → Stages step-gate blocked Save forever). Same OUTCOME as drop-on-save, DIFFERENT root cause → **G-ROUNDTRIP does NOT catch it** (it scans save handlers, not `initial` hydration). Each caught by the review panel, not the gate. | pair `upsert(d, closeOnDone)` (draft-save captures the new id + keeps parent/context) with `*ToDraftSeed(row)` that reverse-maps the FULL row summary (incl. composite/split fields like phone, and gated fields like stages) into a partial draft; don't destructively recompute a summary field when the draft carries none; prefer carrying raw ids on the row over reconstructing from display labels | hydration.mjs (G-HYDRATION) + review-fix checklist | RESOLVED T-020 — SI-4 promotion at 3rd occurrence (hybrid, mirrors T-019). Static scan of `blocks/**/*.block.tsx`: (A) an edit-path `initial={…drafts[…]}` must carry a `*ToDraftSeed(` fallback (else a seeded row edits BLANK); (B) every `onSaveDraft={h}` must thread a `closeOnDone` discriminator (2-arg upsert; resolves bare-identifier wrappers) else repeated draft-save mints duplicates. Red-checked (dropped event seed fallback + 1-arg onSaveDraft passthrough → FAIL(2); clean → PASS). Non-regex-able remainder (seed omits a persisted ROW field like fieldCount / corrupts a composite like phone / a `canProceed` gate blocks a seeded edit — row≠draft) stays a MANDATORY review-fix QA checklist item. Wired into PostToolUse + review-fix + conductor gate chain. Opt out via `// hydration-allow <reason>`. |
| savedraft-no-closeondone | 1 | A wizard block wires `onSaveDraft={submitX}` where `submitX` unconditionally resets `editingId=null` and has no `closeOnDone` param → repeated draft-save / final submit mints DUPLICATE rows (the secondary action does NOT close the sheet). Hit in PipelineConfig (Event/Entity already used the correct `upsert(d, closeOnDone)`). | any `onSaveDraft`-wired block handler MUST be `upsert(d, closeOnDone)` and capture the minted id into `editingId` on draft-save | hydration.mjs (G-HYDRATION, check B) | RESOLVED — folded into G-HYDRATION Check B (T-020): every `onSaveDraft={h}` must thread a `closeOnDone` discriminator (2-arg upsert; resolves bare-identifier wrappers). |
| passive-fragmented-dashboards | 1 | ifm-workforce shipped TWO sibling read-only dashboards (Cockpit + Capacity) where the user wanted ONE live, role-aware, actionable command surface (T-021 → Operations Center › Workforce Pulse on the DispatcherCockpit shell) | when a product's spec smells like "command center" (operators acting on live signals), default to ONE DispatcherCockpit-shaped surface with a role switcher + action queue, not N passive KPI dashboards; dashboards are for analysis, cockpits are for action | none (design-review judgement; not regex-able) | ACTIVE — review-checklist candidate at 2nd occurrence |
| ignored-existing-ds-component | 11 | ifm-workforce Workforce Pulse used a plain Select for a DATE filter while the DS ships DateRangePicker (the canonical FAMS date popup, incl. mode='single') — user flagged: "don't ignore DS; always compare DS for components, functionalities, structures, styling" (T-022) | before authoring ANY control in a product or block, grep the DS barrels (components/basics · primitives · data-display · widgets · data-viz · operations) for an existing component of that shape; only compose new UI when no DS component fits, and say so explicitly in the verdict | none (review-checklist candidate; hard to regex) | PROMOTED (T-024 open) — occurrences: plain Select vs DateRangePicker (T-022); right component WRONG VARIANT (single-date mode where Figma 6649-24799 = preset+range popup); pipelines rendered EntityDetail instead of TaskDetail w/ right timeline panel (T-025); entity-profile Timeline tab hand-rolled rows instead of the DS ActivityFeed (T-026). Checklist item: name the DS component AND variant consumed vs the Figma standard for every surface. 5th occurrence (T-033): bare `<Input placeholder="Search punches…">` with NO leading icon while the DS canonical search field everywhere is SearchMd absolute-positioned + pl-8 + placeholder "Search anything here" — a PATTERN miss, not just a component miss: even when composing from primitives, match the DS composition (icon + copy), never invent ad-hoc placeholder text. 6th occurrence (T-034): product Settings hand-rolled 4 flat pages while the DS ships a full Settings recipe (blocks/settings/settings.block.tsx over src/components/settings/* — Tags/Roles/Apps/Event/Entity/Pipeline config, Preferences, Subscriptions, User Accounts) — SURFACES have DS recipes too, not just controls; check blocks/ before authoring any whole surface. 7th occurrence (T-035): a product filter row bolted ABOVE ContractManagement with EXTERNAL field labels — the FAMS module-toolbar standard is search · filter fields · action on ONE row with the label INSIDE the field ("All Client Types" as the Select's value text); when a DS surface lacks the slot, add a backward-compatible slot prop DS-side (filterSlot/createLabel — DispatcherCockpit precedent), never bolt product chrome. 8th occurrence (T-065 re-review): SchemaForm's `type:'date'` mapped to a NATIVE `<input type="date">` in every schema-driven flow (Inventory assign, Capture Project) while DateRangePicker mode='single' existed — a DS-INTERNAL miss: the DS's own generic form renderer must consume DS components too; fixed at the SchemaForm level so every consumer inherits it. 9th occurrence (T-108, ifm analysis 2026-07-15): ifm duplicated a hand-rolled `emptyRecordsState()` in workforce.tsx + attendance.tsx instead of the DS `EmptyState` (data-display) — PRODUCT-side adoption miss (fix = re-vendor + swap), not a DS gap; reinforces "grep the DS barrels before hand-rolling even an empty state". 10th occurrence (Trip Pass-1 qa review 2026-07-15): trip block seeded `avatarFallback: '🚗'` (raw emoji — font-dependent, breaks FleetRow's text-initials convention) instead of the DS vehicle asset glyph (`src/icons/asset-vectors.tsx`) — caught by qa, fixed in the same pass. Even seed DATA in a block must reuse DS glyphs, not raw emoji. 11th occurrence (Trip Pass-2 design-qa gate 2026-07-15, T-110): the build added a NATIVE `TaskDetail.rightPanelTabs` sub-tab API without noticing `runtime-app.tsx` PipelineDetailBody/RightPanelTabStrip already generalized that slot (live via CRM recipe) — a 2nd mechanism for one job. VARIANT of the class extended from "existing COMPONENT" to "existing MECHANISM/capability": before adding a new API to a shared component, grep for an existing way the component (or its render pipeline) already does it. No regression; consolidation ticketed. |
| ds-overlay-z-layering | 3 | DS floating primitives default z-50 while Sheet/DetailSheet sit at z-[1200] → SelectContent/PopoverContent invisible inside sheets (T-041), Dialog content behind DetailSheet (T-050), DateRangePicker unusable in sheet contexts (T-041 swapped it for a native date input — a documented law deviation pending this fix) | DS-LEVEL FIX DUE (3 hits = promote): floating layers must stack relative to their trigger's layer — either a CSS-var z token cascade or portal-into-sheet; until then per-instance zIndex overrides + restore the DS DateRangePicker in sheets afterwards | z-layers.ts (DS-level) | RESOLVED T-052 (2026-07-12) — shared z tokens (sheet 1200 < dialog 1300 < floating 1320) applied to dialog/alert-dialog/popover/select/dropdown primitives via inline style; per-instance hacks removed; DateRangePicker restored in sheets. |
| dashboard-module-switch | 1 | The T-037 incidents raw-sheet row deep-linked via openModule to the Incidents module — client law: "we don't switch modules for interactions" — drill-ins stack IN PLACE: raw sheet expands to full + the record's TaskDetail opens OVER it (T-054) | cockpit/dashboard drill-ins are sheet-over-sheet stacks in the SAME module; openModule only from a record's own surface | none (mandatory ux lens in review-fix.js) | ACTIVE |
| missing-number-formatting | 1 | Pulse rendered 3206/6492, 8040 etc. without thousand separators at 8k scale (T-054) | every count/quantity ≥1,000 renders via toLocaleString; money compact M/k consistently | none (mandatory ui lens in review-fix.js) | ACTIVE |
| backend-config-on-frontend | 2 | WPS & Employer settings page (T-034) and the Pulse 'Rules in force' strip (T-055) — read-only backend/config identity surfaced as front-end UI; client: "what will user do with rules on front end" | thresholds/identity/config live in Settings (or the backend), never as display strips on dashboards; if a surface needs the value, derive its EFFECT (a breach flag), not the config table | none | PROMOTED at count 2 — ux-designer review lens: 'is any panel just re-printing configuration?' |
| oversized-section-not-widgets | 1 | The Pulse 'Compliance' band packed a gauge + chart + full table + config strip into ONE card (T-055) | dashboard content decomposes into standalone widgets (one concern per card); large tables become fixed-height scrollable widgets with in-widget search (High-Risk-Drivers standard 30836-8263) | none (design review) | ACTIVE |
| module-restructure-overreach | 1 | Asked to fix the 'Visual timesheet' REPORT inside Reports, the T-046 agent restructured the MODULE — renamed it 'Timesheet & Reports', demoted Reports to tab 2 (T-053: "you broke the reports module — properly think before breaking a module") | feedback about a module's CONTENT is never a mandate to change the module's identity/structure (name, rail slot, tab set, default view); a new surface goes where the INFORMATION ARCHITECTURE says it belongs — propose the IA change in the report, don't ship it | none (review judgement) | ACTIVE |
| copy-paste-dataset | 1 | Pulse's Overtime-MTD raw sheet reused the Compliance-rate sheet's columns+rows wholesale — two KPIs, one dataset (T-037) | every KPI's raw-data sheet must present the DATASET THAT PROVES THAT KPI (its own columns + rows); reusing another sheet's table is a data-integrity lie the client spots instantly | none | ACTIVE |
| decorative-unwired-control | 2 | Pulse panel-header dropdowns (All Sites/Today/Live/This week) rendered as controls but had no handler; client: "if dropdowns should not work, just remove them" — kin of the DS ContractManagement Filters-button gap and the T-023 unwired zoom buttons | WIRE OR DELETE: no control ships without a handler; on review, click every visible control once | none (candidate: static scan for filterLabel/onClick-less buttons) | ACTIVE — gate candidate at 2nd occurrence |
| unclear-domain-vocab | 3 | 'Between shifts', 'Buses over', 'Approve relief headcount' — labels only the author understood (T-037); same family as the fleet-flavored banner | every label must pass the 'client reads it cold' test: metric labels state the thing counted, action labels state the concrete action+object+count ("Approve 3 pending leave requests (Dubai Mall)") — 2nd (T-054): sentence-y KPI label 'Open critical incidents' → 'Critical Incidents'; 3rd (2026-07-14): 'Triage' action badge on the same KPI — client: 'what is this triage mean… if too technical maybe remove' — removed (the tile's own click was the real affordance) | none (review judgement) | PROMOTED at count 2 — mandatory ux-designer cold-read lens in review-fix.js |
| heavy-mount-in-event-handler | 1 | Login submit's `setAuthed(true)` mounted the whole AppShell (15 modules + cockpit) as an urgent update → 537ms INP on the click (T-036) | a state update in an event handler that swaps in a HEAVY surface (app mount, module switch, big sheet) must go through `React.startTransition` so the interaction paints before the mount renders; keep validation/error setState urgent | none (perf-trace judgement) | ACTIVE — review-checklist candidate at 2nd occurrence |
| status-color-collision | 2 | Payroll kanban: Approved = chart-4 which is the SAME green (#12B76A) as status-success = Paid; sweep found the identical pair in Lifecycle (Transfer/Promotion vs Permanent) (T-035). 2nd (T-065 QA-3): Employment badges Contract=variant 'info' and Permanent=variant 'secondary' RESOLVE to the same #0072D6 — the collision can hide behind two different VARIANT NAMES; check resolved token values, not variant names (fixed: solid default vs outline — differ in hue AND fill) | within one status/stage SET every color must be pairwise distinct; chart-4 visually equals status-success — treat them as ONE color when assigning; on any stage-color edit, sweep every STAGES array in the product | none (could be a static scan: duplicate color values within a STAGES literal) | ACTIVE — gate candidate at 2nd occurrence |
| rail-nav-duplicated-as-action | 1 | ifm-workforce Operations Center header carried an 'Open live map' icon action that just navigated to the Live Monitoring module — one click away on the module rail already (T-033); user: "why is there a live monitoring icon inside Operation Center when I can access it through module nav" | header/toolbar actions must DO something in-place (mutate, open a sheet, trigger a job); pure navigation to another rail module is never an action — the rail IS the nav; cross-module deep-links are allowed only from data rows (e.g. a worker row → profile), never as standing chrome | none (UX judgement) | ACTIVE — review-checklist candidate at 2nd occurrence |
| token-color-in-non-css-renderer | 2 | Token (`var(--x)`) colors handed to renderers that cannot resolve CSS custom properties: kanban rgba() hex-parser rendered columns fully saturated (T-015); MapLibre canvas `fill-color` rendered zones BLACK (T-031) | any code path that hands a color to a NON-CSS renderer (canvas/WebGL/regex parser/SVG-attr serializer) must resolve tokens first — hex fast-path + getComputedStyle/color-mix fallback; grep candidates: paint:, fill-color, strokeStyle, fillStyle, parseInt(hex | none (each fix is local; promotion = review-checklist line) | PROMOTED at count 2 (SI-4) — added to the review-fix mandatory checklist alongside the ignored-existing-ds-component item: 'does any color reach a non-CSS renderer without token resolution?' |
| vendor-mirror-staleness | 1 | T-065: a prior session's DS mirror-back was written to a bogus literal file `app-shell$f` (botched shell interpolation) so the DS SOURCE silently lacked shipped T-063/T-064 code; a later fixer's faithful "mirror DS→vendor" then DELETED client-verified props (detailOpenCount/toolbarSlot) from the vendor copy — stale source made correct process destructive | before any DS→vendor overwrite, diff the target file against `git show HEAD:vendor/...` (the last VERIFIED state) and treat unexplained deletions as a stop signal; the source of truth for a mirrored pair is whichever side shipped+verified last, not automatically the DS folder; grep for `$`-named stray files after bulk shell writes | none (process rule; candidate: mirror script that refuses net-deletion diffs) | ACTIVE — fixed + recovered same-day (T-065); rule added to learnings |
| locale-formatted-numeric | 3 | THREE same-day client catches (T-065): supervisor rating input showed "3,5", payroll worksheet cells "60,4"/"0,6", wizard value "AED 100.000" — Chromium renders `<input type="number">` with the OS locale's decimal separator and bare `toLocaleString()` follows the OS locale, so numbers rendered comma-decimal / dot-thousands on non-en-US machines | every `<input type="number">` gets `lang="en-US"`; NEVER call bare `toLocaleString()` — always `toLocaleString('en-US')` or the shared nf/aed formatters (src/data/format.ts); wave-close sweep found+fixed 11 bare calls | none (gate candidate: grep `toLocaleString()` bare + number inputs w/o lang) | PROMOTED at count 3 — sweep is now a review-fix lens; gate candidate |
| typography-weight-anomaly | 1 | T-086 (client): pipeline status-control action labels rendered large bold UPPERCASE tracking-wide beside a small status chip; ActivityFeed actor prefixes (SYSTEM/PAYROLL) bold-caps inside an even clock-event list — invented one-off type treatments instead of the DS scale | new controls reuse DS Button/Badge/chip/feed typography — never compose uppercase+tracking+font-bold ad hoc; established caps idioms (THEAD, popup section labels, KPI labels) exempt; review = adjacent-pair screenshot test | none (lens added to all 3 persona notes; gate candidate: grep uppercase+tracking-+font-bold outside established files) | ACTIVE — promote to gate at 2nd occurrence |
| placeholder-surface-shipped | 1 | T-093: ifm-workforce Settings shipped 4 nav entries (Module Management, Appearance, Billing & License, Organization Settings) that rendered a generic `Placeholder` component ("Configuration for X will appear here.") with zero real UI behind the label — client: "hide the settings which don't have UI yet like appearance, billing and don't repeat this mistake of showing empty not added yet settings." Kin of `decorative-unwired-control` (a shipped surface with no substance instead of a control with no handler) — same root cause (composing a placeholder/decoration as if it were done), different shape (a whole nav-reachable PAGE vs a single control). | never compose a settings/nav entry (or any surface) behind a generic `Placeholder`/"coming soon" stub — either the page is fully wired (real state + DS component) or the entry is NOT in the config at all; an empty nav SECTION (all its items hidden) must itself disappear, not render a header over nothing | none (gate candidate: static scan for a shared `Placeholder`/stub component instantiated from a settings/nav items array) | ACTIVE — audit-on-sight item for any settings/admin surface until a 2nd occurrence promotes it |
| search-icon-under-input | 3 | The DS Input primitive/any hand-wrapped input renders a POSITIONED sibling — a `pointer-events-none absolute` search icon composed next to an `<Input>`/`<input>` paints UNDER it wherever the input's bg is opaque (icon present in the DOM the whole time; a DOM check passes while the pixel fails). Occurrences: an early search-field composition law (T-035a, DS canonical pattern documented); T-062 classwide fix (ifm-workforce, 12 external icons + both trees mirrored, client: "why is the search field missing search icon, feels broken"); T-092 CLIENT ESCALATION — the SAME class recurred in the Assign Workers sheet built AFTER the law was written, classwide sweep found 6 more instances (projects/leave/view-renderers×2/events-view) across both trees. | icon absolutely positioned next to an `<Input>`/`<input>` MUST carry `z-10`+; forward law = prefer Input's own `leadingIcon` slot (immune by construction — the span sits inside the SAME component subtree as the native `<input>`); opt out a genuine exception with `// searchfield-allow <reason>` | search-field.mjs (G-SEARCHFIELD) | RESOLVED-BY-GATE (2026-07-14) — SI-4 promotion at the 3rd occurrence. Static scan of DS `src/components/**` (and a product's `src/**` when passed a product root — dual-target, mirrors coherence.mjs's ROOT convention): CHECK A — an icon (`pointer-events-none absolute` in its className) sitting near an `<Input>`/`<input>` must carry `z-10`+, else FAIL (the paint-over class); `primitives/input.tsx`'s own internal leadingIcon composition is excluded (documented immune-by-construction case). CHECK B — an `<Input>`/`<input>` whose placeholder matches /search/i must carry a `leadingIcon` prop or a `Search*` icon in the 3 preceding lines, else FAIL (T-033's bare-input variant, folded in as the same law). Red-checked in an isolated scratch fixture (seeded a z-10-less icon + a bare search input → FAIL(2) w/ file:line, exit 1; reverted → PASS, exit 0). Wired into PostToolUse + review-fix + conductor gate chain. Verification run over the CURRENT DS + ifm-workforce trees found the gate correctly flags report-builder.tsx (2 lines) + report-table.tsx (1 line) as expected-pending (an in-flight fixer owns those) — AND surfaced 24 additional pre-existing instances across the DS (never part of the T-062/T-092 sweep: side-sheet.tsx popovers, AppShell/live-monitoring-view search bars, settings/planning sheets, assignee-picker/parts-table/contract-management/linked-entity-picker, entity-detail/column-config bare inputs) that predate this gate; not fixed here (gate+docs-only scope) — routed to a follow-up sweep ticket. |
| frozen-import-snapshot-picker | 2 | `ACTIVE_WORKERS` (`src/data/workers.ts`: `WORKERS.filter(...)` computed ONCE at module-import time, not a live-store selector) is imported by 17 files. T-038 already fixed ONE instance (Pulse's KPI pool, replaced with a `React.useMemo` over the live `workers` store snapshot) — but T-094 capstone found the SAME frozen import still feeds Inventory's "Assign to" employee picker: live-tested end-to-end (create worker "Zzq QaCapstoneWorker" via Add Employee wizard → confirmed present in Workforce list/search on the SAME browser profile → opened Inventory ▸ Access Card ▸ Assign ▸ searched the exact new-hire name ▸ "No matches", while an existing seeded worker search in the identical field returns results normally) — proves the picker's candidate set is the stale seed array, not the reactive store. | any picker/search/candidate-list sourced from a frozen `export const X = SEED.filter(...)` module constant must be rebuilt as a live selector (`useMemo` over the reactive store snapshot, mirroring `operations-center.tsx`'s `activeWorkers` fix) before being handed to a picker component; grep every consumer of `ACTIVE_WORKERS` (17 files: zone-detail/timesheets/reports/leave/operations-center/incidents/inventory/zones/shifts + data/attendance,timesheet,contracts,shifts,derive,workers,payroll,leave.ts) and audit each for "is this read at import time and handed to user-facing UI" vs "recomputed per-render from live state" | none (gate candidate: static scan for `ACTIVE_WORKERS`/similar frozen-const consumed by a picker/search component prop, not re-selected via useMemo/useStore) | ACTIVE — 2nd confirmed occurrence (Pulse T-038 was the 1st, silently fixed there only); promote-to-gate candidate per the defect→gate rule; full 17-file sweep not yet executed (T-094 found it live in Inventory only — routed to tech-lead) |
| composite-field-independent-default | 1 | Add Employee wizard (`workforce.tsx` `CreateEmployeeWizard`) has TWO separate Visa-expiry inputs for the same concept: Basic Info's own "Visa Expiry" field (top-level `d.visaExpiry`, optional, left blank in the T-094 live test) and the Documents step's "Visa" document entry (`documents[].expiry`, filled with 20 Jul 2026 in the test and correctly saved to the worker's `documents` array — verified via the Documents tab). `store.ts`'s `createWorker` does `visaExpiry: input.visaExpiry ?? '2027-07-10'` — a HARDCODED literal fallback, never derived from `input.documents.find(d => d.type === 'Visa')?.expiry`. Net effect, reproduced + screenshotted: the worker's own profile summary (Overview/Details left rail) shows "Visa Expiry: 10 Jul 2027" while the SAME worker's Documents tab shows "Visa: Expiry Date 20 Jul 2026" for the SAME visa — a same-record, same-session, tab-away-and-back mismatch directly violating the client's "a number shown in one place must reflect elsewhere" mandate. | a summary/composite field that duplicates a value also captured by a structured sub-record (a document, a line item) must either (a) derive from that sub-record at read time, never carry its own independent default, or (b) the wizard must not offer two inputs for one concept — collapse to one source of truth; `createWorker`'s literal-string fallback (`'2027-07-10'`) is the same shape as the earlier composite-field corruption class (User Accounts phone country-code, defect-log "drop-on-load-hydration" note) — a hardcoded default masquerading as a real value while a correct value exists one field away | none (review-checklist: any `?? '<literal-date-string>'` fallback for a field that ALSO exists inside a nested documents/records array is suspect — grep `?? '20\d\d-` in create/save handlers) | ACTIVE — new class, first occurrence; candidate to fold into the existing "drop-on-load-hydration"/composite-field family per the QA persona's 2026-07-10 User Accounts note if it recurs |
| cn-merge-drops-custom-font-size | 1 (systemic — every cn() caller affected) | T-102 (2026-07-17, found in the Smart Routing product after THREE rounds of client "font sizes inconsistent" complaints where the SOURCE read correctly): `cn()` = clsx + tailwind-merge, and tailwind-merge's default config cannot classify the DS's CUSTOM Tailwind-v4 font-size utilities (`text-h1..h6`, `text-body-xl..xs`, `text-body`, `text-label`, `text-caption` — `--text-*` theme tokens). It buckets them as text COLORS, so any later color class in the same cn() call (`cn('text-caption …', 'text-muted-foreground')`) silently DROPS the size — the element renders inherited 16px. Kin of `search-icon-under-input`'s "DOM/source check passes while the pixel fails" trap: grep says the class is there; the RENDERED className lacks it. | `cn.ts` must declare the DS type scale via `extendTailwindMerge({ extend: { classGroups: { 'font-size': [{ text: [h1..h6, body-xl..xs, body, label, caption] }] } } })`; when a font-size "isn't applying," inspect `el.className` in the DOM, not the source; any future custom utility family added to theme.css MUST be registered in cn.ts the same day | none (single-point fix in cn.ts; candidate: a test asserting `cn('text-caption','text-muted-foreground')` keeps both) | RESOLVED T-102 (2026-07-17) — extendTailwindMerge fix in `src/components/utils/cn.ts`, backported FROM the Smart Routing product's vendored copy; DS gates green (typecheck, 64/64 tests, build); showcase visual sweep run same-day (authored sizes now win — previously-dropped sizes surface as intended smaller text). |

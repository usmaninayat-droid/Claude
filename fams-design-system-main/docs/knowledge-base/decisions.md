# Decision Log — locked

Every architecture decision, who made it, and why. Numbered for reference in commits/PRs ("per decision #14").

| # | Decision | By | Why |
|---|---|---|---|
| 1 | **Merge Ben + Shaheer**: Ben's repo = engineering skeleton (monorepo, tokens, tested components, docs); Shaheer's = design truth (Figma-faithful visuals) + low-code runtime (ported in) | Research + founder | They built complementary halves; Ben already adopted Shaheer's token values; token diff proved ~95% byte-identical |
| 2 | **Ben's DTCG tokens canonical; single override: caption = 12px/14px** (Ben carries outdated 10px) | Founder | Token diff found only 7 real differences; founder ruled Ben wins all except caption |
| 3 | **Greenfield React rebuild of v5 — no migration, no Vue interop** | Founder | Standing decision from project start |
| 4 | **v5 is low-code**: all v5 patterns must accept metadata/blueprints as first-class input | Founder | Screens render from per-tenant config (entityconfig) at runtime |
| 5 | **Zero paid libraries/SaaS anywhere** — license-verified free OSS only | Founder | Hard cost rule; AG Grid Enterprise, FullCalendar Premium, Chromatic etc. formally excluded |
| 6 | **Module folders unchanged**: `api/` + `lib/` untouched; keep the name `frontend/` (not "web"); add `contract/` for shared data shapes | Founder | Only the frontend gets rebuilt; contract prevents api/frontend drift |
| 7 | **Headless base: Base UI primary + React Aria for date/time pickers (+Tree) only**; existing Radix migrates on touch; lint bans new Radix. Sanctioned `asChild` mechanism is Base UI's `useRender` (`@base-ui/react/use-render`, see `Text`/`Heading`) — never `@radix-ui/react-slot` in new work | Research, founder approved | shadcn made Base UI its default (Jul 2026); React Aria unmatched for Arabic/RTL/Islamic calendars (Tadweer, Qatar MM) |
| 8 | **One table engine: TanStack Table for ALL tables** (founder rejected RAC for small tables) | Founder | No dual APIs; agents pick wrong ones |
| 9 | **ListView, not "DataView"** — modern list-style UI, not spreadsheet; TanStack Table invisible underneath | Founder | Matches current platform design language |
| 10 | **CreationSheet v1 replicates v5's "first five fields = Basic Info" grouping**; metadata-driven grouping later (do not forget) | Founder | Pilot faithfulness; engine already owns layout so upgrade is clean |
| 11 | **UI hierarchy: components → templates → modules.** Charts/KPI cards are dumb components in ui-kit (no "widgets" layer). Views live in the v5 tier | Founder | UI = dumb only; templates = whole-view constructions (View, EntityProfile, CreationSheet, TaskDetail) |
| 12 | **Naming journey settled**: see [naming.md](naming.md). Composer + blueprints; demo environment (not studio/canvas); skeleton-kit (not app-kit); ui-kit; v5- prefix on the v5 tier; **"core" not "base"** | Founder | Recorded to prevent re-litigating |
| 13 | **All packages live in ONE design-system repo, two tiers**: core (product-agnostic, lint-guarded) + v5 tier. v5-kit included (it's the React rewrite of v5's Vue boot layer, shared by production + demo) | Founder | Tightly-coupled v5 packages version together; one publish pipeline for both consumers |
| 14 | **Multi-tenant demo model: materialized hybrid** — "deltas are the database, resolved is the UI". Typed ops on stable IDs (never JSON-Patch paths). CI: resolve(core+deltas)==resolved | Research (verified vs SAP/Salesforce/Shopify/Kustomize) | Full replicas = the iwmp copy-paste disease; raw deltas = unusable by designers |
| 15 | **No visual/WYSIWYG editor — designers VIBECODE** (tell Claude Code what to change while looking at the rendered app); capture runs at APPROVAL time canonicalizing the locked-in diff into deltas | Founder | The Figma comparison was a metaphor, not a feature |
| 16 | **Three module lifecycles**: core-first (shared-module changes) · tenant-first incubation (new modules built in a tenant, promoted to core when proven — module MOVES, no copy stays, tenant renders 1:1 via core+zero deltas) · tenant-forever (2nd tenant wanting it = promotion trigger) | Founder | Real product motion: ticketing perfected in IWMP; flood mgmt may stay UCCP-only |
| 17 | **Capture execution**: CI check every push; capture as GitHub Action on PR approval (or /capture comment) — commits deltas, posts ops list as PR comment. Failure = red check, PR stays open (never merged-wrong); fallback: agent proposes ops → one-click approve; else merge as override flagged "needs canonicalization" | Founder concern → design | Only designers + agents operate this; must fail loud + harmless |
| 18 | **Demo Console UX**: hover top edge → widget fades in → grid overlay: switch tenant, switch persona (renders as that logged-in user via real privilege gating), jump module, seed reset, share preview link | Founder spec | Personas = seed data (users.json + roles), zero new machinery |
| 19 | **Demo data**: per-module seed JSONs → in-browser relational store (from Shaheer's sim/) → MSW mock API with the production API contract. Coherent cross-module links; reset = re-seed; per-viewer isolated state; fully static deploy | Founder idea, upgraded | Static JSON responses can't handle writes/links |
| 20 | **Small products keep demo/ in-repo** (e.g. fams-hr); v5's demo is a separate repo due to tenant/seed scale + free-roaming agent access. Production paths gated via CODEOWNERS | Founder + me | Shared context, cheaper tokens |
| 21 | **Preview hosting: PARKED** — will be self-hosted OSS, decided later | Founder | Not needed yet |
| 22 | **Performance rules are design-system law**, not module-dev choices — see [tech-stack.md](tech-stack.md) §rules | Perf scan | Enterprise multi-tenant scale requirement |
| 23 | **Security model**: tenant bootstrap registers only licensed modules' routes (chunks never requested); code-splitting is hygiene NOT security; real security = server-side per-endpoint privilege checks (untouched v5 API); per-tenant query cache wiped on logout; UI gating is UX only | Founder question → design | |
| 24 | **Pilot order: entity module first, live monitoring second** | Me, founder aware | Entity module proves the composer (riskiest piece) first |
